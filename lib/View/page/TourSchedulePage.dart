import 'package:flutter/material.dart';

class TourSchedulePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Tour Schedule"),
      ),
      body: Center(
        child: Text("Tour schedule details go here."),
      ),
    );
  }
}
